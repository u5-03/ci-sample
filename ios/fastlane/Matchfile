git_url("git@github.com:user_name/match_repository.git")

storage_mode("git")

type("appstore") # The default type, can be: appstore, adhoc, enterprise or development

app_identifier("com.example.ciSample.debug", "com.example.ciSample") # The bundle identifier of your app

username("sample.mail@mail.com") # Your Apple email address
keychain_name("ci_sample_keychain")
keychain_password("match_keychain_password")

# For all available options run `fastlane match --help`
# Remove the # in the beginning of the line to enable the other options

# The docs are available on https://docs.fastlane.tools/actions/match

# Match passphrase: cmf1mfa7RZA4nmv*rna